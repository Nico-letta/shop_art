{% include 'base.html.twig' %}

{% block block_name %}
    <div class="container">
        <br><br><br>
        <h1>Approvisionnement pour : {{product.name}}</h1>
            <br><br><br>
        {% include 'layouts/_flash_message.html.twig' %}        
        {{form_start(form)}}
                {{form_row(form.quantity, {'attr':{"class":"form form-control"}}) }} <br>
                <input type="submit" class="btn btn-outline-success btn-block">
        {{ form_end(form) }}
    </div>

{% endblock %}